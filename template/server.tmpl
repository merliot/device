// Autogenerated from server.tmpl

package main

import (
	"github.com/merliot/dean"
	"{{.modulePath}}"
)

func main() {
	thing := {{.model}}.New("{{.id}}", "{{.model}}", "{{.name}}").(*{{.model}}.{{.modelTitle}})
	thing.SetDeployParams("{{.deployParams}}")
	server := dean.NewServer(thing, "{{.user}}", "{{.passwd}}", "{{.port}}")
	server.Dials("{{.dialUrls}}")
	server.Run()
}
