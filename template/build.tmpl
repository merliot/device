// Autogenerated from build.tmpl

package main

import (
	"github.com/merliot/dean"
	"github.com/merliot/dean/tinynet"
	"github.com/merliot/device"
	"{{.Module.Path}}"
)

func main() {
	p, _ := device.DeviceParams()
	tinynet.NetConnect(p.Ssid, p.Passphrase)
	thing := {{.Model}}.New(p.Id, p.Model, p.Name).(*{{.Model}}.{{.ModelTitle}})
	thing.SetDeployParams(p.DeployParams)
	runner := dean.NewRunner(thing, p.User, p.Passwd)
	runner.Dials(p.DialURLs)
	runner.Run()
}
